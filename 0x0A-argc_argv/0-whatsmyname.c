#include <stdio.h>
/**
 * main - print name of file
 * @argc:  this function is used count of arguments
 * @argv:  this is a function used to show array of arguments
 * Return: 1
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
